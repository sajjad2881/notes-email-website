//Part of redux stuff 

import { combineReducers } from 'redux';
import notes from './notes';

export default combineReducers({
    //notes: notes,
    //since key and value same can just do it one
    notes 
});